# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: plugin-service
spec:
  selector:
    app: plugin-service
  type: ClusterIP
  ports:
  - name: plugin-service
    protocol: TCP
    port: 80
    targetPort: 6123

